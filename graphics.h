//===== Функции учёта всей графики ==========================================
bool LoadGraphic(void) {
        FILE *ini;
        if((ini = fopen(inifile.c_str(),"r"))==NULL)
                return false;
        
}
//===========================================================================

 